# Always attach (or create) the persistent tmux session "default".
# Fish inside that session skips a second exec because $TMUX is set
# (see fish/config.fish). Homebrew paths cover macOS GUI launches.
exe_search_path /opt/homebrew/bin
exe_search_path /usr/local/bin
shell tmux new-session -A -s default
